# =========================================================
# 基础设施配置 —— 仅保留“连哪个数据库”这类无法放 DB 的引导项
# 所有业务配置（host / headers / redis / default_parameters
# / encryption_decryption / ui_element_list / appium / auth 等）
# 已迁移到配置中心数据库（config_store 表），由前端 UI 管理。
# =========================================================

[pytest]
log_cli = true
log_cli_level = INFO

# ⚠️ 已废弃 — PostgreSQL 为统一标准，SQLite 不再维护
# [sqlite_local]
# type = sqlite
# database = sqlite.db
# path = data/db/sqlite.db

[postgres_local]
type = postgresql
host = 127.0.0.1
port = 5432
user = tap
password = tap_pass
database = tap
