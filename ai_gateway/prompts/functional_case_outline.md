你是一名资深测试工程师。请通读下面的需求信息（可能含文档摘录、界面截图），为「{{MODULE_NAME}}」模块**规划测试点大纲**——只列测试点，不写详细步骤。

# 需求 / 输入信息
{{REQUIREMENT_TEXT}}

# 当前项目其它模块（用于交叉考量）
{{CROSS_MODULE_CONTEXT}}

# 本模块已有用例（重要：不要重复这些，只规划「新增 / 尚未覆盖」的测试点）
{{EXISTING_CASES}}

# 要求

0. **只规划新增/补充的测试点**：上面「已有用例」覆盖到的场景**不要再列**；如果用户的需求是给本模块补充新功能点，请聚焦在新功能点及其与已有功能的交互上。
1. **全面覆盖**：正向主流程、异常（错误输入/边界/为空/超长等）、不同角色/权限差异、状态流转、以及与其它模块的交叉影响。宁可多列，不要漏。
2. 每个测试点用**一句话标题**概括，并标注类别（正向 / 异常 / 边界 / 权限 / 跨模块 / 其它）。
3. **额外产出一段「需求摘要 digest」**：把需求的关键信息浓缩成自洽、完整的文字——包含涉及的页面、关键控件（输入框/按钮/菜单）、角色、重要业务规则；**如果有截图，要把图里看到的页面结构和控件也写进摘要**。这段摘要会被后续「分批生成详细用例」复用，所以必须信息完整、不依赖原图也能看懂。
4. 测试点数量由覆盖度决定，不要凑整。

# 输出格式（严格 JSON，只输出一个 ```json``` 代码块，不要任何额外文字）

```json
{
  "digest": "本模块为管理员工作台登录。涉及登录页（用户名输入框、密码输入框、登录按钮），登录成功后按角色跳转：管理员→管理员工作台，普通用户→普通工作台。部分功能未完成，入口显示占位提示。规则：密码错误提示且不跳转；连续错误 5 次锁定……",
  "points": [
    {"title": "管理员用合法账号登录成功并跳转管理员工作台", "category": "正向"},
    {"title": "普通用户登录后看到的菜单与管理员不同", "category": "权限"},
    {"title": "密码错误时提示且停留登录页", "category": "异常"},
    {"title": "未完成功能入口显示占位提示", "category": "其它"}
  ]
}
```

约束：返回值必须是能被 `json.loads` 解析的合法 JSON 对象，含 `digest`(字符串) 和 `points`(数组)，数组每项含 `title` 和 `category`。不要在 JSON 外写解释。
