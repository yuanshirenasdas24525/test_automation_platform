# =============================================================================
# 测试自动化平台 —— 一键起 docker-compose
#
# 起：    docker compose up -d
# 停：    docker compose down
# 重建：  docker compose up -d --build
# 看日志: docker compose logs -f api
#         docker compose logs -f worker
#
# 默认端口暴露：
#   8000 → FastAPI（前端 SPA + /api/*）
#   6379 → Redis
#   5432 → Postgres
#
# 想跑 App 自动化（Android/iOS）需要再起 Appium 容器；这里没默认带，
# 因为模拟器/物理机情况各异，参见 docker-compose.legacy.yaml.bak 里
# 注释掉的 android_emulator + appium_server 块作为参考。
# =============================================================================
x-app-common: &app-common
  build: .
  image: ghcr.io/yuanshirenasdas24525/test_automation_platform:latest
  environment: &app-env
    TZ: Asia/Shanghai
    PYTHONUNBUFFERED: "1"
    # 生产环境必须设置（≥32 位随机串）；不设则回退到配置中心/默认值
    JWT_SECRET_KEY: ${JWT_SECRET_KEY:-}
    # CORS 允许来源，默认空（收紧）。api 容器同源托管前端时无需设置；
    # 前后端分域部署时填 "https://foo.com,https://bar.com"。
    BACKEND_CORS_ORIGINS: ${BACKEND_CORS_ORIGINS:-}
    LOG_LEVEL: ${LOG_LEVEL:-INFO}
    # Web Recorder Agent 原生运行在宿主机，容器通过 host.docker.internal 控制。
    UI_RECORDER_AGENT_URL: ${UI_RECORDER_AGENT_URL:-http://host.docker.internal:54352}
    UI_RECORDER_AGENT_SECRET: ${UI_RECORDER_AGENT_SECRET:-}
    # 项目未显式配置测试账号 API 地址时，API/worker 都通过 compose 服务名访问平台。
    WEB_TEST_ACCOUNT_API_BASE_URL: ${WEB_TEST_ACCOUNT_API_BASE_URL:-http://api:8000}
    # 探活
    REDIS_HOST: redis
    REDIS_PORT: "6379"
    # Web 自动化：容器内无显示器，强制无头模式；本地调试时可 docker run -e WEB_HEADLESS=false
    WEB_HEADLESS: "true"
    # Celery broker / backend
    CELERY_BROKER_URL: redis://redis:6379/0
    CELERY_RESULT_BACKEND: redis://redis:6379/1
    # 平台 DB —— 使用 PostgreSQL
    DB_HOST: postgres
    DB_PORT: "5432"
    DB_USER: ${DB_USER:-tap}
    # DB_PASSWORD 无默认值：未在 .env / 环境变量里显式设置时，compose 直接报错拒绝启动，
    # 避免全网复用同一个 git 里就能看到的弱口令。
    DB_PASSWORD: ${DB_PASSWORD:?请在 .env 或环境变量中设置 DB_PASSWORD}
    DB_NAME: ${DB_NAME:-tap}
  volumes: &app-vols
    # 数据 / 日志 / 报告 / app 包都挂出来，container 重建不丢数据
    - ./data:/app/data
    - ./config:/app/config
    # Playwright 浏览器内核缓存（chromium 等），重建镜像后不用重新下载
    - playwright_cache:/root/.cache/ms-playwright
  depends_on:
    redis:
      condition: service_healthy
    postgres:
      condition: service_healthy
  restart: unless-stopped
  extra_hosts:
    - "host.docker.internal:host-gateway"


services:
  # -------------------------------------------------------------------------
  # 1. Redis —— Celery broker + result backend
  # -------------------------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: tap_redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  # -------------------------------------------------------------------------
  # 2. Postgres —— 平台元数据库唯一支持的运行方式
  # -------------------------------------------------------------------------
  postgres:
    image: postgres:15-alpine
    container_name: tap_postgres
    environment:
      POSTGRES_USER: ${DB_USER:-tap}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?请在 .env 或环境变量中设置 DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-tap}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "tap"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  # -------------------------------------------------------------------------
  # 3. FastAPI（API + 前端 SPA）
  # 启动时会自动跑 alembic upgrade head（entrypoint 里）
  # -------------------------------------------------------------------------
  api:
    <<: *app-common
    container_name: tap_api
    command:
      - uvicorn
      - server.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --workers
      - "2"
    ports:
      - "8000:8000"

  # -------------------------------------------------------------------------
  # 4. Celery worker —— 跑用例 / 设备探活 / 报告聚合
  # 用 -c 控制并发；本地调试可以改 1
  # -------------------------------------------------------------------------
  worker:
    <<: *app-common
    container_name: tap_worker
    command:
      - celery
      - -A
      - celery_app
      - worker
      - --loglevel=INFO
      - -c
      - "4"
    # worker 不监听 HTTP，关掉默认健康检查
    healthcheck:
      disable: true

  # -------------------------------------------------------------------------
  # 5. Celery beat —— 定时任务调度器（probe_devices 等）
  # 一个集群只能起一个 beat，否则会重复触发
  # -------------------------------------------------------------------------
  beat:
    <<: *app-common
    container_name: tap_beat
    command:
      - celery
      - -A
      - celery_app
      - beat
      - --loglevel=INFO
      - --schedule
      - /app/data/celerybeat-schedule.db
    healthcheck:
      disable: true

  # -------------------------------------------------------------------------
  # 6. Appium Server —— App 自动化（Android / iOS）
  # Appium 2 镜像，预装 UiAutomator2（Android）和 XCUITest（iOS）驱动。
  # 注意：
  #   - Android 需要 adb 连通设备/模拟器，推荐 network_mode: host 或挂载 adb socket
  #   - iOS 需要 macOS 宿主机 + Xcode，无法在 Linux 容器内运行
  #   - 连接方式：在 devices 表注册设备时，agent_host 填 appium-server 的 host，
  #     appium_port 填 4723
  # -------------------------------------------------------------------------
  appium:
    image: appium/appium:latest
    platform: linux/amd64
    container_name: tap_appium
    # Appium 无鉴权，暴露到 0.0.0.0 等于把连接设备/宿主机的任意操作（装 APK、
    # adb shell、文件读写）开放给同网段任何人。只绑本机回环；worker/api 通过
    # compose 内部网络用服务名 appium:4723 互访，不受此影响。
    # 确需跨机访问时，请在前面加带鉴权的反向代理，不要直接放开这个端口。
    ports:
      - "127.0.0.1:4723:4723"
    volumes:
      # adb key 持久化（Android 设备授权）
      - appium_adb_keys:/home/androidusr/.android
      # 可选：挂载 APK/IPA 目录（安装 app 步骤用）
      - ./data/app_packages:/app/app_packages:ro
    environment:
      TZ: Asia/Shanghai
      # Android：保持 adb 连接存活
      ADB_SERVER_SOCKET: tcp:host.docker.internal:5037
      # Android/iOS 驱动自动安装（首次启动从 npm 拉）
      APPIUM_DRIVER_UIAUTOMATOR2: "true"
      APPIUM_DRIVER_XCUITEST: "true"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:4723/status"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

  # -------------------------------------------------------------------------
  # 7. Android Emulator（可选 —— 资源占用很大）
  # 如果不想连真机，可以用容器化模拟器。需要宿主机支持 KVM。
  # 取消注释下面这组配置，同时把 appium 的 ADB_SERVER_SOCKET 改成 emulator 地址。
  # -------------------------------------------------------------------------
  # android_emulator:
  #   image: budtmo/docker-android:emulator_13.0
  #   container_name: tap_android_emulator
  #   privileged: true
  #   ports:
  #     - "6080:6080"     # noVNC 网页控制台
  #     - "5554:5554"     # adb connect
  #     - "5555:5555"
  #   environment:
  #     DEVICE: "pixel_6"
  #     EMULATOR_ARGS: "-no-window -no-audio -gpu swiftshader_indirect"
  #     APPIUM: "true"
  #   volumes:
  #     - android_emulator_data:/data
  #   restart: unless-stopped


volumes:
  redis_data:
  postgres_data:
  playwright_cache:
  appium_adb_keys:
